                $ref: '#/components/schemas/Error'
                
  /api/projects:
    get:
      tags:
        - Project & Task Management
      summary: List all projects
      description: Get paginated list of projects (ADMIN, SUPER_ADMIN only)
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
        - name: status
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Projects retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Success'
                  - type: object
                    properties:
                      data:
                        type: object
    post:
      tags:
        - Project & Task Management
      summary: Create project
      description: Create a new project (ADMIN, SUPER_ADMIN only)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - title
              properties:
                title:
                  type: string
                description:
                  type: string
                status:
                  type: string
                  enum: [planning, in_progress, completed, on_hold]
                startDate:
                  type: string
                  format: date
                endDate:
                  type: string
                  format: date
                assignedUserIds:
                  type: array
                  items:
                    type: string
      responses:
        '201':
          description: Project created successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Success'
                  - type: object
                    properties:
                      data:
                        type: object
                        
  /api/projects/{id}:
    get:
      tags:
        - Project & Task Management
      summary: Get project
      description: Get project details (ALL if assigned)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Project retrieved
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Success'
                  - type: object
                    properties:
                      data:
                        type: object
    put:
      tags:
        - Project & Task Management
      summary: Update project
      description: Update project (ADMIN only)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                description:
                  type: string
                status:
                  type: string
                startDate:
                  type: string
                endDate:
                  type: string
                assignedUserIds:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Project updated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Success'
                  - type: object
                    properties:
                      data:
                        type: object
    delete:
      tags:
        - Project & Task Management
      summary: Delete project
      description: Delete project (ADMIN only)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Project deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
                
  /api/projects/{id}/tasks:
    post:
      tags:
        - Project & Task Management
      summary: Add task
      description: Add task to project (ADMIN only)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - title
                - assignedUserId
              properties:
                title:
                  type: string
                description:
                  type: string
                assignedUserId:
                  type: string
                dueDate:
                  type: string
                  format: date
                priority:
                  type: string
                  enum: [low, medium, high]
      responses:
        '201':
          description: Task created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Success'
                  - type: object
                    properties:
                      data:
                        type: object
                        
  /api/tasks/{id}:
    get:
      tags:
        - Project & Task Management
      summary: Get task
      description: Get task details (ASSIGNED USERS)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Task retrieved
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Success'
                  - type: object
                    properties:
                      data:
                        type: object
                        
  /api/tasks/{id}/status:
    patch:
      tags:
        - Project & Task Management
      summary: Update task status
      description: Update task status (ASSIGNED USER only)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum: [pending, in_progress, completed, review]
      responses:
        '200':
          description: Status updated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Success'
                  - type: object
                    properties:
                      data:
                        type: object
                        
  /api/tasks/{id}/submit:
    post:
      tags:
        - Project & Task Management
      summary: Upload work file
      description: Submit work for task (ASSIGNED USER only)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - fileUrl
                - fileName
              properties:
                fileUrl:
                  type: string
                  format: uri
                fileName:
                  type: string
                notes:
                  type: string
      responses:
        '201':
          description: Work submitted
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Success'
                  - type: object
                    properties:
                      data:
                        type: object
                        
  /api/tasks/{id}/feedback:
    post:
      tags:
        - Project & Task Management
      summary: Add feedback
      description: Add feedback to task (ADMIN, TEACHER only)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - feedback
              properties:
                feedback:
                  type: string
                rating:
                  type: number
                  minimum: 1
                  maximum: 5
      responses:
        '201':
          description: Feedback added
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Success'
                  - type: object
                    properties:
                      data:
                        type: object
                        
  /api/tasks/user/{userId}:
    get:
      tags:
        - Project & Task Management
      summary: Get user's assigned tasks
      description: Get all tasks assigned to a user (ALL)
      security:
        - bearerAuth: []
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
        - name: status
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Tasks retrieved
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/Success'
                  - type: object
                    properties:
                      data:
                        type: object
